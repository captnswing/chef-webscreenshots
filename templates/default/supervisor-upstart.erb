description     "supervisor"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

chdir <%= node["webscreenshots"]["venv_home"] %>

exec <%= node["webscreenshots"]["venv_home"] %>/bin/supervisord -u <%= node["webscreenshots"]["user"] %> --nodaemon --configuration <%= node["webscreenshots"]["venv_home"] %>/etc/supervisord.conf
